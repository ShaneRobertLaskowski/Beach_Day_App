<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="beach_day.BeachSelectPage">
    
    <ContentPage.Content>
        
        <StackLayout Padding="6">

            <Picker x:Name="BeachPicker" Title="Select A Beach..." 
                    SelectedIndexChanged="Beach_Picked"
                    ItemDisplayBinding ="{Binding Name}">
            </Picker>

            <Image Source="poweredbyoneline.png"
                   VerticalOptions="CenterAndExpand" 
                   HorizontalOptions="Center"
                   WidthRequest="200" HeightRequest="50"/>
            <RelativeLayout HeightRequest="150">
                <ListView x:Name="WeatherList" RowHeight="150" Rotation="270" SeparatorVisibility="None"
                          RelativeLayout.XConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width, Factor=0.5, Constant=-75}"
                          RelativeLayout.YConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width, Factor=-0.5, Constant=75}"
                          RelativeLayout.WidthConstraint="{ConstraintExpression Type=Constant, Constant=150}"
                          RelativeLayout.HeightConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width, Factor=1}"
                          CachingStrategy="RecycleElement">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <ViewCell.ContextActions>
                                    <MenuItem Text="More Info" Clicked="MenuItem_MoreInfo_Clicked"  CommandParameter="{Binding .}" />
                                </ViewCell.ContextActions>
                                <ContentView Rotation="90">
                                    <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" Orientation="Vertical">
                                        <Label Text="{Binding Day}"></Label>
                                        <Label Text="{Binding Month}"></Label>
                                        <Label Text="{Binding TemperatureHigh}"></Label>
                                        <Label Text="{Binding TemperatureLow}"></Label>
                                        <Label Text="{Binding Summary}"></Label>
                                    </StackLayout>
                                </ContentView>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </RelativeLayout>

            <Button Text="Get Directions" VerticalOptions="Center" 
                    HorizontalOptions="Center" Clicked="DisplayDirections"/>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>